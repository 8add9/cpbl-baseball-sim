services:
  baseball-sim:
    build: .
    container_name: cpbl-baseball-sim
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - path: .env.backend
        required: false
    volumes:
      - baseball-sim-data:/data

volumes:
  baseball-sim-data:
